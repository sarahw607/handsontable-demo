export enum ModalFullscreenAnimation {
  On = 'on',
  Off = 'off',
}

export enum ModalVisibility {
  Open = 'open',
  Closed = 'closed',
}

export enum ColumnType {
  Checkbox = 'checkbox',
  Date = 'date',
  Numeric = 'numeric',
  Text = 'text',
}

export enum ValueType {
  Enum = 'ENUM',
  Money = 'MONEY',
  Number = 'NUMBER',
  Text = 'TEXT',
}

export enum SortOrder {
  Ascending = 'ASC',
  Descending = 'DESC',
}
